{
  "name": "graiman-pwa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:host": "vite --host",
    "build": "vite build",
    "build:github": "vite build && echo. > dist/.nojekyll",
    "preview": "vite preview",
    "preview:host": "vite preview --host",
    "deploy": "npm run build:github && gh-pages -d dist"
  },
  "dependencies": {
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "gh-pages": "^6.3.0",
    "vite": "^7.1.2",
    "vite-plugin-pwa": "^1.0.2"
  }
}
